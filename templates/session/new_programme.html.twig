{# {% extends 'base.html.twig' %}

{% block title %}Ajouter un programme à la session {{ session.intitule }}{% endblock %}

{% block body %}
<div class="container">
    <h1>Ajouter un programme à la session {{ session.intitule }}</h1>
    
    {{ form_start(form) }}
    {{ form_row(form.nbJours) }}
    {{ form_row(form.module) }}
    
    <button type="submit" class="btn btn-primary">Ajouter</button>
    {{ form_end(form) }}
</div>
{% endblock %} #}

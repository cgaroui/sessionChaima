{% extends 'base.html.twig' %}

{% block title %}Profil de l'utilisateur{% endblock %}

{% block body %}
<div class="container">
    <h1 class="mt-4">Profil de l'utilisateur</h1>

    <div class="card">
        <div class="card-header">
            Informations sur le profil
        </div>
        <div class="card-body">
            <h5 class="card-title">Pseudo : {{ username }}</h5>
            <p class="card-text">Email : {{ email }}</p>
            {# <p class="card-text">Rôles : 
                {% for role in roles %}
                    <span class="badge bg-primary">{{ role }}</span>
                {% endfor %} #}
            </p>
        </div>
    </div>

    <a href="{{ path('app_formation') }}" class="btn btn-primary mt-4">Retour</a>
</div>
{% endblock %}

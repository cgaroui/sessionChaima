{% extends 'base.html.twig' %}

{% block title %}Profil de l'utilisateur{% endblock %}

{% block body %}
<div class="card">
    <div class="card-header">
        Informations sur le profil
    </div>
    <div class="card-body">
        <h5 class="card-title">Pseudo : {{ user.pseudo }}</h5>
        <p class="card-text">Email : {{ user.email }}</p>
        <p>Mot de passe :
            <a href="{{ path('user_edit', {id: user.id}) }}" class="card-text">modifier</a>
        </p>
    </div>
</div>

    <a href="{{ path('app_formation') }}" class="btn btn-primary mt-4">Retour</a>
</div>
{% endblock %}

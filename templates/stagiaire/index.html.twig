{% extends 'base.html.twig' %}

{% block title %}Liste des stagiaires{% endblock %}

{% block body %}
    <h1>Liste des stagiaires</h1>

    {% for stagiaire in stagiaires %}
    <a href="{{path('show_stagiaire', {'id':stagiaire.id})}}">{{ stagiaire.nom }} {{ stagiaire.prenom }}</a>
    <br>
    {% endfor %}
{% endblock %}
